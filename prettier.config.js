// @ts-check

/** @type {import('prettier').Options} */
const config = {
    tabWidth: 4,
    semi: false,
    arrowParens: "avoid",
    bracketSpacing: true,
    trailingComma: "none"
}

module.exports = config
